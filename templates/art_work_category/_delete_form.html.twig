<form method="post" action="{{ path('app_art_work_category_delete', {'id': art_work_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ art_work_category.id) }}">
    <button class="btn">Delete</button>
</form>
