{% extends 'base.html.twig' %}

{% block title %}ArtWorkCategory{% endblock %}

{% block body %}
    <h1>ArtWorkCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ art_work_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ art_work_category.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ art_work_category.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_art_work_category_index') }}">back to list</a>

    <a href="{{ path('app_art_work_category_edit', {'id': art_work_category.id}) }}">edit</a>

    {{ include('art_work_category/_delete_form.html.twig') }}
{% endblock %}
